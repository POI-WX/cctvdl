# Changelog

本项目的所有重要变更记录在此文件中。

## 0.6.3

### 修复

- 修复节目列表超过固定分页范围时可能缺失后续内容的问题，栏目、节目集、看点、片段及 `v.cctv.com` 历史目录均会完整读取。

### 优化

- 长期归档按月份浏览时会直接定位目标分页；央视返回的总数或排序不可靠时自动回退到完整扫描，避免静默遗漏。
- Release 构建在上传前自动检查应用架构、解密资源及内置 ffmpeg，阻止不完整或架构不匹配的安装包发布。

## 0.6.2

### 优化

- 统一主要操作的尺寸、字重、圆角和键盘焦点样式，优化深色选中状态、已选内容面板及下载队列的状态层级。
- 节目详情的下载按钮固定在右下角；简介较长时改为区域内滚动，不再将下载操作推出页面。
- 节目集排序改为「从早到晚 / 从晚到早」；加载看点和片段后，三类内容仍按发布时间统一排序。
- 「保存设置」仅在内容发生修改后启用，保存完成后恢复为不可用状态并显示保存时间。

## 0.6.1

### 新增

- 支持 `v.cctv.com` 历史节目目录。
- 可选择在节目列表中加载并标记看点和片段。
- 视频预览新增频道和时长信息。

### 修复

- 完善旧专题、节目概览、导视片段和独立视频的识别，避免节目集与单视频误分类。
- 修复部分旧节目集的正片识别、分页遗漏、时间顺序和节目名称问题。
- CCTV-16 优先使用可用的明文 HLS，失败时自动回退到原有下载方式。

## 0.6.0

### 修复

- 统一新旧栏目及 `VIDA` 历史归档的节目顺序，均按发布时间从早到晚展示，不再受央视接口返回方向差异影响。
- 「下载选中」成功提交后自动移除对应已选项，无需等待下载完成后手动清空。
- 下载提交期间锁定操作，并按视频 `guid` 忽略已在排队或下载中的重复任务，避免连续点击造成重复下载或干扰现有任务。
- 全部任务因已下载而被跳过时不再伪造批次完成事件，避免错误清空正在运行的下载状态或覆盖自动打开目录设置。
- 「重新下载」会明确绕过下载历史过滤，同时继续保留对排队中和下载中任务的去重保护。

### 优化

- 下载提示改为显示实际加入及跳过的任务数量；已下载或已在队列中的选择会给出明确反馈。
- 统一已选、排队、下载中、已完成和重新下载之间的状态语义。

### 测试

- 补充旧栏目逆序响应、批量选择消费、全量跳过不结束现有批次及强制重新下载的回归测试。

## 0.5.7

### 新增

- **节目月份快速定位**：按月栏目可一键定位最早或最新节目月份，停更栏目和跨度较长的历史归档无需逐月查找。

### 修复

- 收紧跨栏目 ID 判断：只有稳定归档同时包含当前页面栏目 ID 和其他栏目 ID 时才视为迁移，避免独立电影因页面栏目与专辑栏目不同而误判为空栏目。
- 批量加入下载队列的确认框改为显示「自动（最高画质）」「蓝光 1080p」等用户可读名称，不再暴露 `auto`、`bluray` 等内部值。

### 优化

- 跨年度 `VIDA` 归档按目标月份从更近的首尾方向分页加载并使用短期缓存，切换到较新月份无需从最早一期遍历全部节目。
- 剪贴板自动导入改为应用级常驻监听；启用或应用界面就绪后立即检查，缩短轮询间隔，并按顺序处理连续复制的链接，在设置页和下载页也不会漏掉提示。

### 文档

- 更新 README、使用指南和常见问题，说明节目月份快速定位、电影误判修复及剪贴板提示行为；补充常规央视与央视新闻的准确画质规格、选择规则和兜底说明，并优化表格排版。

### 测试

- 补充栏目迁移、电影误判、按月分页与月份边界、剪贴板即时检查和设置页常驻监听测试，并覆盖真实央视链接 GUI / E2E 场景。

## 0.5.6

### 新增

- **跨栏目 ID 的历史归档**：长期节目即使中途更换 `TOPC`，节目概览及新旧任意一期也会统一导入为同一个按月栏目，并通过稳定 `VIDA` 数据源显示完整历史内容。
- **历史央视子域名**：常规页面支持 `cctv.com`、`cctv.cn`、`cntv.cn` 及其频道子域名，同时严格校验域名边界。

### 修复

- 重新导入曾被保存成节目集的跨 ID 长期栏目时，会升级为按月栏目并保留收藏状态；旧版备份不会覆盖已升级的数据源信息。

### 优化

- `VIDA` 列表按月筛选并使用短期缓存；切月无需重复遍历数百期，手动刷新仍会重新获取。

### 文档

- 更新 README、使用指南、常见问题和贡献指南，说明历史子域名、跨 `TOPC` 归档、按月数据源及兼容迁移规则。

## 0.5.5

### 新增

- **节目概览页导入**：电视剧、纪录片等节目现在可直接从节目首页导入，不必先找到任意一集；粘贴某一集仍会自动识别整部节目。
- **文档截图自动生成**：新增脚本，以真实央视页面生成 README 和使用指南所用的界面图片，后续界面变化可一键更新文档。

### 修复

- 避免同一视频重复进入下载队列或覆盖已有同名文件；合并失败时自动清理临时输出。
- 拖入链接与剪贴板导入共用支持范围，补全 `tv.cctv.cn`、`news.cctv.cn` 等已支持页面。
- 读取已保存的栏目、单视频、下载记录和待恢复任务时会跳过损坏条目；导入损坏的 JSON 会给出清楚提示。

### 优化

- 下载页进度只统计当前批次；可按栏目查看跨栏目已选视频并逐条移除；节目集会边加载边显示选集，并可切换排序方式。
- 收藏节目在启动时只检查最新一页选集，避免长节目集拖慢启动。

### 文档

- 重写 README、使用指南和常见问题，补齐节目集、已选内容、下载恢复、队列管理和设置说明，并使用当前真实界面截图。

## 0.5.4

### 修复

- **节目集下载范围**：选集页不再显示只适用于栏目的「下载本月」，避免将全部选集误认为当月内容。
- **下载本月范围**：严格下载当前栏目的当月完整列表，不再受搜索过滤或跨月、跨栏目勾选状态影响。
- **央视新闻画质与重新下载**：导入和从下载历史重新下载时，都会按当前清晰度设置重新选择画质。
- **任务恢复与取消**：重启时只恢复未完成任务；取消下载会等待当前任务停止，并且每轮下载只会结算一次。
- **边界处理**：全部任务被跳过后不会错误打开文件夹；节目集分页重复时会自动停止请求。

### 优化

- **跨栏目批量选择**：切换栏目时保留已勾选视频，可与跨月选择一起通过「下载选中」批量加入队列；列表提示改为「当前列表」子计数。
- **列表排序与选集完整性**：常规栏目改为从早到晚展示；节目集自动翻页并去重，避免超过 100 集时遗漏内容。
- **央视新闻下载**：明文 HLS 分片遵守并行下载配额，并补齐重试、断点续传与历史重新下载来源。

## 0.5.3

### 新增

- **跨月 / 跨栏目批量选择**：同一栏目切换月份可累积勾选；跨栏目也可继续累积；footer 显示跨月累计 + 本月子计数。
- **下载队列追加语义**：`startBatch` → `appendJobs`，后续下载追加到现有队列，仅当协调器空闲时才重置批次统计；`currentBatchAutoOpen` 改 OR 累积。

### 修复

- 修正「替换语义」相关 3 个 bug：切 tab 后月份重置为当月、下载页卡片被替换而非追加、重试单任务误清空队列。

### 测试

- 新增跨月 / 跨栏目 / 央视新闻 e2e 元数据测试（4 用例）；新增 content store / coordinator / download store / IPC 共 18 个新用例。

## 0.5.2

### 移除

- **预览面板折叠按钮**（0.2.3 引入）：原承诺"折叠后视频列表全宽显示"，但 `.home-sidebar` 一直固定 360px，折叠仅让右侧预览区缩为 28px 窄条，无实际宽度再分配价值；按钮本体仅 20×20px 且半透明，多数用户找不到。删除按钮、`previewCollapsed` 状态、相关 CSS、过渡动画与 U2 GUI 测试。

### 文档

- **README 措辞收敛**：标题下 slogan 突出差异化价值；去掉重复的「高清」与第 4 次出现的「支持 Windows / macOS / Linux」；📥 内容管理分组内同名子弹改名为「收藏与备份」；「单视频直链」子弹 52 字压到 33 字；「高清画质」末尾与画质无关的 ffmpeg 信息归位到「下载安装」callout。

## 0.5.1

### 新增

- **央视新闻移动端视频下载**：支持 `cctvnews.cctv.com/snow-book/{video,index}.html?item_id=...` 链接，通过 Emas 网关（HMAC-SHA256 签名 + base64 响应解码）获取视频元数据与多档 m3u8，按用户在设置中选择的清晰度档位挑选最佳一档下载。一条央视新闻移动端链接可能包含多条视频，全部自动加入「📌 单个视频」集合。
- **未加密 HLS 下载路径**：下载协调器新增 `m3u8Url` 分支——对于预解析好的明文 m3u8（当前仅由 cctvnews 提供），直接并发下载 `.ts` segment 并用 ffmpeg 无损合并，**跳过 CCTV 解密子进程**；常规 CCTV 加密流路径保持不变。
- **`resolveVideoBatch` IPC**：新增批量解析接口返回 `VideoInfo[]`，统一处理单视频页与多视频央视新闻文章；`resolveSingleVideo` 保留向后兼容。
- **错误消息本地化**：`humanizeError` 新增 cctvnews 专属规则（接口空响应 / base64 解码 / Emas 网关 / 缺少 `item_id`），并补充「无法解析视频信息」规则对齐已有节目解析错误。

### 优化

- **文件大小格式化统一**：`formatFileSize` 改为委托 `formatBytes`，消除重复的 KB/MB/GB 逻辑。

### 测试

- 新增 `tests/unit/api/cctvnews.test.ts`：覆盖签名已知向量、`fetchArticle` / `resolveFromUrl` mock 响应（含多画质 / 多视频 / 空响应 / base64 失败）、`isCctvNewsSnowBookPage` 形态、`pickQuality` 带宽选择。
- `browse.test.ts` 与 `coordinator.test.ts` 分别扩展 cctvnews 路由与 `m3u8Url` 分支的成功 / m3u8 失败 / segment 失败三种路径。

## 0.5.0

### 新增

- **新闻文章视频页**：支持导入 `news.cctv.cn` / `news.cctv.com` 中带视频的新闻文章页，解析 `videoCenterId` 后获取真实单视频信息并下载。
- **节目集 / 选集型内容**：支持电视剧、4K 纪录片等按 `album_id` 组织的页面，导入后左侧复用内容列表，右侧显示「选集」列表并隐藏月份选择器。

### 修复

- **复杂视频页分类**：先识别栏目、节目集、片段单视频与普通单视频，再进入对应流程，避免 4K/电视剧误判为普通单视频，也避免导视/精彩片段误导入为空栏目。
- **单视频元数据**：单视频标题优先使用视频 API 标题，保留完整节目标题；发布时间优先使用接口返回的完整时间，列表分组仍按日期归组。
- **guid 解析**：移除 URL 中 `VIDE...` / `ARTI...` 作为可播放 guid 的兜底逻辑，必须使用页面 HTML 或新闻文章元数据中的真实 guid。

### 测试

- 补充栏目、节目集、新闻文章页、导视/精彩片段、完整时间与 `focus_date` 兼容的单元测试；增加真实链接联网 smoke 和节目集 GUI 覆盖。

## 0.4.5

### 重构 / 优化

- **死代码清理**：删除从未调用的 navigate 函数、sortedPrograms 死导出；formatBytes 取消导出；__pinia 暴露加 NODE_ENV 守卫
- **配置修复**：tsconfig.json 删除无效 paths/declaration/outDir 配置，exclude 修正为 dist-electron；vitest.config.ts 显式排除 tests/gui/**；.gitignore 补充 .claude/
- **注释清理**：修正 decryptor.ts 矛盾注释；删除 8 处纯 WHAT 注释及推测性/断言性注释
- **代码质量**：finalizer.ts uniquePath 循环耗尽时抛出错误（避免静默覆盖）；coordinator.ts 提取 4 个命名常量；http.ts 添加 DEFAULT_REFERER 常量；HLSVariant.resolution 改为具名对象
- **冗余合并**：提取 applyDarkMode 到 dark-mode.ts；画质选项改为 v-for + QUALITY_LABELS；formatBytes 内部化
- **架构改进**：新建 cover.ts 提取封面下载逻辑；ErrorCode/AppError/parseError 简化为单一 humanizeError 函数
- **测试补全**：extractTitle 补 5 个边界用例；新增 cover.ts 单元测试（7 个）；settings.test.ts 缩进修正
- **文档修复**：FAQ 链接改为 GitHub issue 创建页面；USAGE 并发描述修正；CONTRIBUTING 模块列表补全
- **README 重构**：功能列表从 15 条平铺改为 4 组分类（内容管理 / 下载引擎 / 文件与历史 / 使用体验）

## 0.4.4

### 优化

- **UI 文案统一**：下载页阶段文字去技术术语（「下载视频片段」「合并中」「准备中」「N/N 段」）；「选中的视频缺少 GUID」改为「选中的视频链接无效」
- **设置项改名**：「并发下载数」→「下载线程数」，「并行下载视频数」→「同时下载视频数」，描述全部精简，面向普通用户
- **错误提示统一**：视频保存目录相关 warning 措辞统一；封面下载网络异常返回中文提示
- **文档与截图全量更新**：README/USAGE/FAQ 去掉 CDN、分片、线程等技术术语；所有截图重拍，主图改用新闻联播真实数据

## 0.4.3

### 新增

- **保存封面**：预览面板操作栏（复制简介右侧）新增「🖼 保存封面」按钮，一键将视频封面图片保存到本地
- **图片保存目录**：设置页新增「图片保存目录」配置项（默认系统图片文件夹），与视频保存目录独立管理，方便归档
- 设置页「文件保存位置」统一改名为「视频保存目录」，与图片目录命名对称

## 0.4.2

### 修复 / 优化

- **videoset URL 解析**：新增从页面内 CCTV AJAX URL 字符串提取 TOPC id 的正则回退，`/videoset/` 等不暴露 `var` 声明的栏目页（如经济半小时）现可正常导入
- **栏目名多余「视频」后缀**：清洗 `<title>` 时追加去除「节目视频」「视频」「节目」后缀，同时移除冗余正则，修复 videoset 页面解析出「经济半小时视频」的问题
- **重新下载静默跳过**：设置页「↺ 重新下载」改用 `retryJob`（绕过历史去重），确保主动触发的重新下载真正入队
- **月份选择器不刷新**：`el-date-picker` 补加 `@change`，手动选月后自动刷新视频列表
- **月份「本月」按钮**：将月份快捷按钮「今」改为「本月」，语义更清晰
- **删除弹窗文案**：删除栏目确认弹窗「节目」改为「栏目」
- **红点自动消除**：点击收藏栏目后新内容红点即时消失

## 0.4.1

### 修复

- **播放按钮打开文件夹**：修复下载完成后点「▶ 播放」实际打开文件夹的问题（`applyJobFinished` 补全 `sourceJob` 赋值）
- **虚拟滚动切换不回顶**：切换栏目、月份或单视频集合时，列表不再停留在旧滚动位置

## 0.4.0

### 新增 / 优化

- **任务排序**：下载队列中排队中的任务支持拖拽（`⠿` 句柄）和「↑ 置顶」按钮调整优先级，顺序持久化到下次启动
- **历史搜索与重新下载**：设置页下载历史新增关键词搜索框；每条记录可直接点「↺ 重新下载」重新入队
- **虚拟滚动**：视频列表超过 100 条时自动切换轻量虚拟滚动，避免大量历史视频时卡顿
- **托盘失败提示**：批次下载有任务失败时，系统托盘图标标注提醒（macOS 标题 `⚠`，其他平台 tooltip），5 秒后自动复位

## 0.3.5

### 优化 / 修复

- **并行下载线程均分**：调整为每路视频实际使用 `floor(threadCount / concurrentVideos)` 个线程，总并发保持不变，避免随视频数倍增
- **设置页文案优化**：所有描述去掉技术内部术语（「分片」「CDN」「去重」），标点统一，面向用户友好表达
- **按钮 UI 重设计**：「保存设置」与「下载此集/视频」按钮改为胶囊形圆角，加柔和阴影与 hover 上浮动效
- **代码修复**：消除产品代码中唯一一处 `any` 类型；修复 `resumePending([])` 空数组时误触发 `batchFinished` 的 bug
- **测试补充**：补 `removeFromDownloadHistory`、`concurrentVideos` clamp、`resumePending` 空数组守卫、`threadCount` 均分共 5 个新测例（单元测试 → 377 个）
- **文档更新**：README/USAGE/FAQ 补充并行下载、侧边栏快捷键（`Ctrl+\`）、历史详情、线程均分行为等新功能说明
- **截图重拍**：`home.png`、`home-dark.png`、`settings.png` 对应最新 UI

## 0.3.4

### 新增

- **多任务并行下载**：设置页新增「并行下载视频数」滑块（1–3），支持多个视频同时下载；默认 1（串行，与原来一致）。注：0.3.5 已将每路视频的线程数改为均分（`floor(threadCount / concurrentVideos)`），不再正交叠加。

## 0.3.3

### 新增 / 优化

- **侧边栏体验全面升级**：折叠按钮移至顶部；`el-tooltip` 即时提示（含快捷键）；新增 `Ctrl+\` 键盘切换；下载进行中嵌入迷你进度条和实时速度显示
- **下载按钮重设计**：footer「下载本月」降为幽灵按钮、「下载选中」升为主色按钮（带数字角标）；单视频模式不再显示「下载选中」；「下载此集/视频」统一高度规格，有批量选中时弱化并提示
- **「保存设置」优化**：从右上角大按钮移至页面底部 sticky footer，尺寸与页面控件统一，已保存时间戳以绿色✓渐入显示
- **程序列表自适应**：从固定 180px 高度改为 flex 自适应，支持 15+ 栏目时内部独立滚动

## 0.3.2

### 新增 / 优化

- **下载历史详情**：设置页历史记录升级为列表视图，展示标题、完成时间（相对）、文件大小、文件名；支持单条删除（🗑）与定位文件（📂）
- **下载页观感**：速度数字加粗独立行、进度条条纹动画（下载中）、排队序号（第 N 位）、批次完成右下角通知、所有卡片支持右键菜单（复制标题对所有卡片可用；播放 / 定位仅已完成卡片显示；重试仅失败卡片显示）

## 0.3.1

### 新增

- **单视频集合备份**：「📌 单个视频」集合支持从 JSON 导出备份 / 从 JSON 导入恢复，与栏目备份对称；集合表头新增 ↓/↑ 操作按钮
- **下载历史富模型**：历史记录从仅存 guid 升级为含标题、文件路径、文件大小、完成时间的完整条目；自动兼容迁移旧格式
- **下载队列持久化**：应用异常退出后重启可自动恢复上次未完成的排队任务，不再需要重新手动选择下载

## 0.3.0

### 优化

- **代码质量与性能整体优化**：修复 App.vue 重复订阅 IPC 事件导致 store 被调用两次；下载进度回调三次 `segments.filter` 合并为单次遍历；收藏栏目新内容检查改为并发请求；类型全面收紧（`JobState`/`JobStage`/`Quality` 替换 `string`）；冗余 computed 从已有派生消除重复计算

## 0.2.5

### 新增 / 优化

- **Pinia 状态管理**：`useAppStore` / `useDownloadStore` / `useContentStore` 统一组件状态，IPC 事件监听集中在 store action 中
- **结构化错误码**：`errors.ts` 新增 `ErrorCode` 枚举与 `AppError` 类型，`humanizeError` 签名向后兼容
- **单视频封面与简介修复**：优先从 `getHttpVideoInfo` API 获取真实封面（fmspic 截帧）和简介，不再显示 CCTV 官网通用占位图
- **单视频 guid 修复**：优先取页面 HTML 中的 `var guid`（实际可下载 guid），而非 URL 中的 VIDE token（CMS 内容 ID）
- **僵尸栏目检测**：导入栏目时自动检测是否有历史内容，无内容则回退到单视频下载（修复停播节目与独立电影被误导入为空栏目的问题）
- **首页视频区加宽**：左侧内容面板从 300px 加宽至 360px，视频行标题不再截断

## 0.2.4

### 新增

- **应用内版本更新检测**：启动时后台检查 GitHub 最新版本，有新版本时侧边栏品牌图标显示红点，关于弹窗出现「发现新版本，点击前往下载」链接（仅检测，不自动下载）
- **收藏栏目新内容提醒**：启动时后台检查收藏栏目本月最新视频，有未下载内容时栏目行显示红点提示

## 0.2.3

### 新增 / 优化

- **视频列表缩略图**：每行左侧显示小封面（40×30px，懒加载），`coverUrl` 缺失时自动隐藏
- **已下载标记增强**：视频行右侧补淡绿色 `✓` 图标，配合原有绿色左边框，一眼扫视更清晰
- **切栏目骨架屏**：切换栏目加载时替换为 `el-skeleton` 动态占位，告别空白等待
- **预览面板可折叠**：右侧预览面板新增 `‹/›` 折叠按钮，折叠后视频列表全宽显示
- **下载页空状态引导**：空队列时新增「去首页选择视频」按钮，一键跳转
- **月份指示器**：切月后若无视频，月份选择器旁显示橙色圆点提示「本月暂无视频」

## 0.2.2

### 新增

- **剪贴板自动导入**（设置开关，**默认关闭**）：开启后，复制央视栏目 / 视频链接时弹窗提示导入；仅在开启后才会读取剪贴板，关闭时不读取

## 0.2.1

### 新增

- **JSON 导入栏目**：栏目表头 `↓` 从 JSON 文件导入栏目（与导出备份对称）
- **下载本月**：栏目列表「下载本月」一键勾选并下载当前栏目本月全部期数
- **下载完成后打开文件夹**（设置开关，默认关）：仅「下载本月」或单视频集合模式下的下载完成时自动打开保存目录；列模式下的「下载选中」与「下载此集」不触发，避免频繁弹出

## 0.2.0

### 新增

- **单视频直链下载**：粘贴不属于任何栏目的独立视频（如电影）链接，自动识别并直接下载
- 左侧「我的栏目」升级为「我的内容」，新增常驻「📌 单个视频」集合——单视频持久保存，可预览 / 下载 / 移除，与栏目浏览隐式切换

## 0.1.2

栏目管理增强与特殊栏目修复。

### 新增

- **收藏栏目**：⭐ 置顶常用栏目（最近收藏的排最前），列表按「收藏 / 全部」分组显示
- **批量清空栏目**：表头 🧹 一键清空（带确认）
- 栏目行悬停显示收藏 / 删除按钮，删除不再只能靠右键

### 修复

- macOS 下 `Delete` 键无法删除栏目（现支持 `Delete` 与 macOS 的 `Backspace`）
- 在搜索框中按删除键会误删选中栏目（输入框聚焦时不再触发删除快捷键）
- 「等着我」等无标准 `column_id` 的特殊栏目页现明确报错，不再导入取不到视频的空栏目

## 0.1.1

macOS 安装与使用问题修复。

### 修复

- 修复 macOS 安装包提示「已损坏，无法打开」：改用 ad-hoc 签名（升级 electron-builder v26），现可在「系统设置 → 隐私与安全性」底部点「仍要打开」放行
- 修复 macOS 下输入框无法使用 Cmd+C/V/X/A 复制粘贴：补充原生 Edit 菜单

### 文档

- 更新 README / FAQ 的 macOS 安装与放行说明，补充终端 `xattr` 临时绕过方式

## 0.1.0

cctvdl 的首个公开版本：一款开箱即用的跨平台央视视频下载器。以下是本版本提供的能力。

### 下载

- 导入栏目 / 节目链接，按月浏览、关键词搜索，封面与简介预览
- 多选批量下载，串行队列执行，任务卡片实时显示进度、速度与剩余时间
- 自动处理央视加密节目，下载后即可正常观看
- 清晰度可选：流畅 / 标清 / 高清 / 超清 / 蓝光（最高 1080p），或「自动」取最高，缺档自动降级
- 下载完成自动合并为 MP4（默认无损快速，可选兼容重编码）
- 断点续传：失败 / 取消后保留进度，重试自动续传
- 下载历史去重，重复内容自动跳过

### 管理与界面

- 下载页分「进行中 / 已完成 / 失败·取消」三组；完成后可一键播放（系统播放器）或定位文件
- 垂直侧边栏（可展开 / 收起）、深色模式、6 种主题色、封面大图预览
- 拖放导入、键盘快捷键（`1`/`2`/`3`、`F5`、`Ctrl+A`、`Delete`、`Esc`）、系统托盘进度提示、窗口大小 / 位置记忆
- 设置：保存位置、并发数、清晰度、合并方式、深色模式、主题色、日志级别 / 目录、下载历史管理

### 开箱即用

- 支持 Windows / macOS / Linux
- 安装包已内置所需运行时与 ffmpeg，无需任何额外安装或配置

### 质量

- 三层测试：单元测试 + GUI 自动化测试 + 端到端联网管线测试
- CI 在三平台逐层把关（lint / typecheck / build → 单元 → GUI）；端到端联网测试因 CDN 封境外 IP 改为手动触发
- ESLint；打 `v*` tag 时自动构建并发布三平台安装包
